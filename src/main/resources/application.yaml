spring:
  r2dbc:
    url: r2dbc:pool:postgres://localhost:5432/postgres
    username: postgres
    password: postgres
#    properties:
#      sslMode: REQUIRE #TODO

logging:
  level:
    org:
      springframework:
        data:
          r2dbc: DEBUG
      liquibase: DEBUG
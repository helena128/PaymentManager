spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      username: admin
      password: password
      authentication-database: admin
      database: payments

logging:
  level:
    org:
      springframework:
        data:
          mongo: DEBUG
    com.helena128.paymentmanager: DEBUG